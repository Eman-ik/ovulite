---
phase: 00-foundation
plan: 02
type: execute
wave: 2
depends_on: ["00-01"]
files_modified:
  - backend/app/models/__init__.py
  - backend/app/models/donor.py
  - backend/app/models/sire.py
  - backend/app/models/recipient.py
  - backend/app/models/technician.py
  - backend/app/models/protocol.py
  - backend/app/models/embryo.py
  - backend/app/models/et_transfer.py
  - backend/app/models/embryo_image.py
  - backend/app/models/protocol_log.py
  - backend/app/models/prediction.py
  - backend/app/models/anomaly.py
  - backend/app/models/user.py
  - backend/app/database.py
  - backend/alembic.ini
  - backend/alembic/env.py
  - backend/alembic/versions/001_initial_schema.py
autonomous: true

must_haves:
  truths:
    - "All 11 database tables exist in PostgreSQL after migration"
    - "Canonical ET view (vw_et_features) can be queried"
    - "Alembic can upgrade and downgrade migrations"
    - "SQLAlchemy models match the schema in REQUIREMENTS.md"
  artifacts:
    - path: "backend/app/models/__init__.py"
      provides: "All SQLAlchemy model imports"
      exports: ["Donor", "Sire", "Recipient", "Technician", "Protocol", "Embryo", "ETTransfer", "EmbryoImage", "ProtocolLog", "Prediction", "Anomaly", "User"]
    - path: "backend/app/database.py"
      provides: "Database engine, session factory, Base"
      exports: ["engine", "SessionLocal", "Base", "get_db"]
    - path: "backend/alembic/versions/001_initial_schema.py"
      provides: "Initial migration with all tables + canonical view"
  key_links:
    - from: "backend/app/models/*.py"
      to: "backend/app/database.py"
      via: "Base import"
      pattern: "from.*database.*import.*Base"
    - from: "backend/alembic/env.py"
      to: "backend/app/models/__init__.py"
      via: "target_metadata"
      pattern: "target_metadata.*Base.metadata"
    - from: "backend/app/database.py"
      to: ".env"
      via: "DATABASE_URL"
      pattern: "DATABASE_URL"
---

<objective>
Create all SQLAlchemy 2.0 ORM models and Alembic migration infrastructure for the Ovulite database.

Purpose: Establish the complete data layer so that API endpoints (Plan 04) and data ingestion (Phase 1) have a working database with all 11 tables + canonical view.
Output: SQLAlchemy models for all entities, Alembic migration creating full schema, database.py connection setup.
</objective>

<execution_context>
.github/skills/execute-plan/SKILL.md
@.gsd/templates/summary.md
</execution_context>

<context>
@PROJECT.md
@REQUIREMENTS.md (Section 2: Database Schema Design — all CREATE TABLE statements and vw_et_features view)
@.gsd/phases/00-foundation/00-01-SUMMARY.md (for backend directory structure)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Database connection + SQLAlchemy Base + all ORM models</name>
  <files>
    backend/app/database.py
    backend/app/models/__init__.py
    backend/app/models/donor.py
    backend/app/models/sire.py
    backend/app/models/recipient.py
    backend/app/models/technician.py
    backend/app/models/protocol.py
    backend/app/models/embryo.py
    backend/app/models/et_transfer.py
    backend/app/models/embryo_image.py
    backend/app/models/protocol_log.py
    backend/app/models/prediction.py
    backend/app/models/anomaly.py
    backend/app/models/user.py
  </files>
  <action>
    Create `backend/app/database.py`:
    - Use SQLAlchemy 2.0 style (DeclarativeBase)
    - Read DATABASE_URL from environment (os.getenv with fallback)
    - Create engine with pool_pre_ping=True
    - Create SessionLocal with sessionmaker
    - Create get_db() dependency generator (yields session, closes after)
    - Define Base class

    Create one model file per table in `backend/app/models/`:
    - Translate EVERY table from REQUIREMENTS.md §2.1 exactly:
      1. donors — tag_id unique, breed, birth_weight_epd, timestamps
      2. sires — name, breed, birth_weight_epd, semen_type (CHECK constraint), timestamps
      3. recipients — tag_id, farm_location, cow_or_heifer (CHECK), timestamps
      4. technicians — name unique, role, active boolean, created_at
      5. protocols — name unique, description, created_at
      6. embryos — donor_id FK, sire_id FK, opu_date, stage (1-9), grade (1-4), fresh_or_frozen, cane_number, freezing_date, ai_grade, ai_viability, timestamps
      7. et_transfers — ALL columns from REQUIREMENTS.md including: et_number, lab, satellite, customer_id, et_date, farm_location, recipient_id FK, bc_score, cl_side, cl_measure_mm, protocol_id FK, heat_observed, heat_day, embryo_id FK, technician_id FK, assistant_name, pc1_date, pc1_result (CHECK: Pregnant/Open/Recheck), pc2_date, pc2_result, fetal_sexing, days_in_pregnancy, timestamps
      8. embryo_images — embryo_id FK, file_path, file_hash, width_px, height_px, uploaded_at, notes
      9. protocol_logs — transfer_id FK, protocol_id FK, step_name, step_date, drug_name, dosage, notes, created_at
      10. predictions — transfer_id FK, model_name, model_version, probability, confidence_lower, confidence_upper, risk_band, shap_json (JSON type), predicted_at
      11. anomalies — anomaly_type, entity_type, entity_id, severity, description, metric_value, baseline_value, detected_at
      12. users — username unique, password_hash, role (CHECK: admin/veterinarian/embryologist/viewer), full_name, email, active, created_at, last_login

    Use proper SQLAlchemy 2.0 patterns:
    - `Mapped[type]` with `mapped_column()`
    - Relationships with `relationship()` and `back_populates`
    - Use `JSONB` type from `sqlalchemy.dialects.postgresql` for shap_json
    - Use `CheckConstraint` for enum-like fields
    - Use `func.now()` for server defaults on timestamps

    Create `backend/app/models/__init__.py` that imports all models so Alembic picks them up.
  </action>
  <verify>python -c "from app.models import *; print('All models imported successfully')" (run from backend/ directory)</verify>
  <done>13 model files exist; all import without errors; models match REQUIREMENTS.md schema exactly</done>
</task>

<task type="auto">
  <name>Task 2: Alembic setup + initial migration + canonical view</name>
  <files>
    backend/alembic.ini
    backend/alembic/env.py
    backend/alembic/script.py.mako
    backend/alembic/versions/001_initial_schema.py
  </files>
  <action>
    Initialize Alembic in backend/ directory:
    - Create alembic.ini with sqlalchemy.url pointing to DATABASE_URL env var
    - Create alembic/env.py that:
      - Imports Base.metadata from app.database
      - Imports all models from app.models (so autogenerate works)
      - Sets target_metadata = Base.metadata
      - Reads DATABASE_URL from environment for both online and offline modes
    - Create alembic/script.py.mako (standard template)

    Create initial migration `alembic/versions/001_initial_schema.py`:
    - Use autogenerate or manual `op.create_table()` for all 11 entity tables + users table
    - Include ALL constraints (CHECK, UNIQUE, FOREIGN KEY)
    - Add `op.execute()` at the end to create the `vw_et_features` canonical view (from REQUIREMENTS.md §2.2)
    - Downgrade should drop view first, then all tables in correct FK order

    **Important:** The canonical view SQL is in REQUIREMENTS.md Section 2.2. Copy it exactly into the migration's upgrade() using op.execute().
  </action>
  <verify>cd backend; alembic check (or alembic upgrade head against running DB)</verify>
  <done>alembic.ini exists; env.py references all models; migration creates 12 tables + 1 view; downgrade drops everything cleanly</done>
</task>

</tasks>

<verification>
1. All 13 model files import without errors
2. `alembic upgrade head` creates all tables in PostgreSQL
3. `SELECT * FROM vw_et_features LIMIT 0` succeeds (view exists)
4. `alembic downgrade base` drops everything cleanly
5. Models match REQUIREMENTS.md §2 schema exactly
</verification>

<success_criteria>
- 12 SQLAlchemy models faithfully represent REQUIREMENTS.md schema
- Database connection reads DATABASE_URL from environment
- Alembic migration creates all tables + canonical view
- get_db() dependency available for FastAPI endpoints
</success_criteria>

<output>
After completion, create `.gsd/phases/00-foundation/00-02-SUMMARY.md`
</output>
